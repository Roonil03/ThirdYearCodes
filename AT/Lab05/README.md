# Lab exercises:
1. Create a ListView using ListTile to display student details.
2. Design a GridView.builder to show images.
3. Create a Stack layout with text over an image.
4. Implement a CustomScrollView with SliverGrid.
5. Develop a Flutter app to fetch and display product details from an API.
6. Create a Flutter application to retrieve student data from Firebase.

# Additional Questions:
1. Card Widget
    - Screenshot showing a centered Card with elevation and text.
    - AppBar titled "Card Example" visible at the top.
2. ListView and ListTile
    - Screenshot showing a vertical scrolling list.
    - Each row contains a person icon, student name, and department.
3. GridView
    - Screenshot displaying a 2-column grid.
    - Colored containers (red, green, blue, orange) arranged in grid format.
4. GridView.count
    - Screenshot showing a grid with 3 columns.
    - Each grid cell displayed as a Card with text Item 0, Item 1, etc.
5. GridView.extent
    - Screenshot showing responsive grid layout.
    - Number of columns changes based on screen size.
6. GridView.builder
    - Screenshot of dynamically generated grid items.
    - Smooth scrolling with repeated card widgets.